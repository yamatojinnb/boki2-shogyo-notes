---
// 共通ヘッダー：スクロール追従＋すりガラス
import { base } from '../lib/site';
---

<header class="site-header">
  <div class="container inner">
    <a class="brand" href={base}>
      <span class="brand-mark" aria-hidden="true">簿</span>
      <span>
        <span class="brand-name">会計ノート</span><br />
        <span class="brand-sub">ACCOUNTING NOTES</span>
      </span>
    </a>
    <nav class="site-nav" aria-label="サイト内">
      <a href={base}>簿記2級</a>
      <a href={`${base}ifrs/`}>IFRS</a>
      <a href={`${base}mock/`}>模擬試験</a>
    </nav>
  </div>
</header>
